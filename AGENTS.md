# 작업 우선순위

- 모든 크롤러 수집 파일(PDF·EPUB·ZIP·이미지·보존 원본 등)의 최초 저장 루트는 `G:\내 드라이브\0\_받는함\크롤러`이다. 사용자가 `0_받는함`이 아닌 `0\_받는함`을 새로 만들도록 확인했다. 기존 작업별 하위 폴더 구조는 유지한다. 기존 파일을 삭제하거나 덮어쓰지 않는다.

- 사용자가 최우선으로 지정한 기능은 `/pdf` 화면의 **파일·설명 페이지·미러 URL 다운로드**이다.
- 이 기능의 회귀를 조사할 때는 기존 작업 브랜치 `fe56b9c`와 현재 코드의 요청·미러 탐색·저장 경로부터 비교한다. 특정 URL 하나에만 맞춘 해결을 목표로 삼지 않는다.
- 코드 차이와 실행 환경 차이를 구분하고, 테스트 통과를 실다운로드 성공으로 보고하지 않는다.
- 사용자가 매번 AI에게 실행을 요청하지 않도록 `run.bat`으로 독립 실행할 수 있게 유지한다. 이미 실행 중이면 중복 서버를 띄우지 않고 PDF 화면을 연다.
- PDF 시트 입력창은 설명·번호·표 등과 URL이 섞인 텍스트를 허용한다. HTTP(S) URL만 추출하고 입력 내 중복을 제거하되 URL의 쿼리·다운로드 키는 보존한다.
- 핵심 사용 흐름은 시트에 URL을 모아두고 앱을 켜두면 밤새 자동 저장되는 것이다. Google 인증·토큰 갱신·시트 읽기와 결과 기록까지 검증하며, 브라우저 승인 응답만으로 연결 성공을 선언하지 않는다. PC 절전/종료 중에는 처리되지 않는 한계를 안내한다.
